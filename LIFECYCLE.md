## Stability Guarantees

The following are considered stable in Epimnesis v0.1:

- Memory layers and their meaning
- Retrieval semantics and scoring factors
- The `retrieve` API and its guarantees
- The SDK lifecycle contract

Any change to the above requires a major version bump.
