{
  "name": "engine",
  "version": "1.0.0",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/epimnesis/epimnesis.git"
  },
  "scripts": {
    "format": "pnpm format:rs && pnpm format:toml",
    "format:toml": "taplo format",
    "format:rs": "cargo fmt",
    "test": "cargo test"
  },
  "devDependencies": {
    "@taplo/cli": "^0.7.0",
    "lint-staged": "^16.1.6",
    "typescript": "^5.9.2"
  },
  "lint-staged": {
    "*.toml": [
      "taplo format"
    ]
  }
}
